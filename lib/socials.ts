import type { Platform } from "./types";

/**
 * Social accounts for the footer. Empty for now — add an entry like
 * { name: "Instagram", url: "https://instagram.com/...", icon: "generic" }
 * and it'll show up automatically. New icon shapes go in components/icons.tsx.
 */
export const SOCIALS: Platform[] = [];
